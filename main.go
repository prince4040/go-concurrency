package main

import "fmt"

/*
The repository root is a course index, not a concurrency example. Each numbered
directory is an independent command so learners can run and modify one concept
at a time.
*/

func main() {
	fmt.Println("Choose a lesson, for example: go run ./1.routine")
	fmt.Println("See README.md for the complete learning path.")
}
